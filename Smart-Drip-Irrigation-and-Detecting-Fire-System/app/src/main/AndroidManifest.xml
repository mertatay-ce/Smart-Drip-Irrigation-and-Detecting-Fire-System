<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.smart_drip_irrigation_and_detecting_fire_system">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartDripIrrigationandDetectingFireSystem">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SmokeDensityActivity"></activity>
        <activity android:name=".DirtCheckActivity"></activity>
        <activity android:name=".AirTemperatureActivity"></activity>
        <activity android:name=".AirHumidityActivity"></activity>
    </application>

</manifest>